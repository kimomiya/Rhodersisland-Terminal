import 'package:flutter/material.dart';

class RhodesAppBar extends StatelessWidget {
  const RhodesAppBar({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return AppBar();
  }
}
